export interface Grid {
  color: string;
}

export interface LayoutGrid {
  columnWidth: number;
  gutterWidth: number;
  beatHeight: number;
  columnsAmount: number;
  beatMultiplier: number;
  maxWidth: number;
}
